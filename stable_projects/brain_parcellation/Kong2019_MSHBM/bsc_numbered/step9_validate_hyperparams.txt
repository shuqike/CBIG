% copy your priors folder from <output_dir>/estimate_group_priors into <output_dir>/generate_individual_parcellations
% open Matlab command line right in CBIG folder
project_dir='/home/pbfs18/Documents/shuqi_code/data_bases/bsc_numbered_output_GSP_parc/generate_individual_parcellations';
w_set = [20 40 60 80 100 120];
c_set = [5 10 20 30 40 50 60 80 100 120];
num_clusters='17';
num_sess='5';
for i = 1:length(w_set)
    for j = 1:length(c_set)
        for sub = 1:2
            homo_with_weight(sub,:) = CBIG_MSHBM_parameters_validation(project_dir,'fsaverage5',num_sess,num_clusters,num2str(sub), num2str(w_set(i)),num2str(c_set(j)));
        end
        homo(i,j) = mean(mean(homo_with_weight));
    end
end
% best w=100, c=30, homogeneity=0.4357